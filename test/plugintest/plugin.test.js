const Vue = require('vue');
const plugin = require('../../src/plugin');
const assert = require('assert');
let expect = require('expect.js');
describe('vego-plugin', function () {
    describe('should inject to components and applied in condition', function () {
        Vue.use(plugin);
        const testcase = new Vue();
        

    });
});
